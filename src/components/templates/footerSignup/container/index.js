import React from 'react';
import FooterSignupPresentation from '../presentation';

const FooterSignupContainer = props => {
  return <FooterSignupPresentation {...props} />;
};

export default FooterSignupContainer;
