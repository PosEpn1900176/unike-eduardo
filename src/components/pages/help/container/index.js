import React from 'react';
import HelpPagePresentation from '../presentation';

const HelpPageContainer = props => {
  return <HelpPagePresentation {...props} />;
};

export default HelpPageContainer;
